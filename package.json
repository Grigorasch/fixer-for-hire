{
  "name": "worker",
  "version": "0.0.1",
  "description": "Small task manager. Allows you to create a queue of objects that need to be processed, and then, when you decide, start processing the backlog of cases.",
  "main": "./src/worker.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "worker",
    "task manager"
  ],
  "author": "Grigory",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  }
}
